# Required: your Anthropic API key. Get one at https://console.anthropic.com/
# Used only server-side by netlify/functions/investor-chat.js — never exposed to the frontend.
ANTHROPIC_API_KEY=

# Optional: override the Claude model used for investor responses.
CLAUDE_MODEL=claude-sonnet-5
